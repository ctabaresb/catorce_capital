# =============================================================================
# requirements-ecs.txt
# Full Python dependencies for ECS Fargate tasks.
# Used by: backfill, backtest engine, GBM simulation engine.
#
# These are heavier than the Lambda layer requirements because ECS has no
# size limits and needs scipy + cvxpy for portfolio optimization (Week 2).
# =============================================================================

# HTTP client
requests==2.31.0
urllib3==2.0.7

# Data processing
pandas==2.1.4
pyarrow==14.0.2
numpy==1.26.3

# Portfolio optimization (Week 2)
scipy==1.11.4
cvxpy==1.4.1

# AWS SDK
boto3==1.34.14
botocore==1.34.14
