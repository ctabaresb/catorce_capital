# requirements.txt — x-tweets Lambdas (Python 3.12)
# -------------------------------------------------
# These functions do not bundle third-party libraries.
# - boto3/botocore are provided by the AWS Lambda Python 3.12 runtime.
# - `requests` is provided by a shared Lambda layer (not via this file).
#
# If you ever stop using the requests layer and want to bundle the dependency
# directly with the function code, just uncomment the line below:
#
# requests>=2.31.0,<3.0.0
