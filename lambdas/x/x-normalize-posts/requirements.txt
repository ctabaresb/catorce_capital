# requirements.txt — x-normalize-posts (Python 3.12)
# --------------------------------------------------
# This Lambda only uses:
# - Python stdlib (json, gzip, datetime, etc.)
# - boto3, which is provided by the AWS Lambda runtime.
#
# HTTP client `requests` is provided via a shared Lambda layer
# (e.g. requests-py312), so we don't bundle it here.
#
# If you ever remove the requests layer and need extra libs,
# list them below.
